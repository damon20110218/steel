			// params.clear();
			// if (i == 0) {
			// tof = true;
			// if (fs.getClientId() == null) {
			// String maxIdSql = "select max(client_id) from client_info";
			// Long maxId = jdbcTemplate.queryForObject(maxIdSql,
			// params.toArray(), Long.class);
			// clientId = maxId + 1;
			// String clientSql = "insert into client_info(client_id,
			// client_name, client_type, create_date) values (?, ?, ?,?)";
			// params.add(clientId);
			// params.add(fs.getClientName());
			// params.add("1");
			// params.add(now);
			// jdbcTemplate.update(clientSql, params.toArray());
			// }
			// }